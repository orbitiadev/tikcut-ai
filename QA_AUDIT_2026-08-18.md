# TikCut AI browser QA audit

This branch exists to execute the current production code through GitHub Actions with a real Chromium browser and a synthetic 65-minute video fixture.

Coverage includes long-source metadata, silence scan, short MP4 export, transcript scoring, caption styles, invalid-file handling, pre-upload guards, the 10-minute export safety cap, local persistence, Storyverse continuation/export, Supabase signed-out/connectivity checks, and mobile layout.
